#ifndef __STRING_ADD_H__
#define __STRING_ADD_H__

#include <stdlib.h>

// assumes left and right are naught strings with a 
// preceding length field
char* string_add(char* left, char* right);

#endif /* __STRING_ADD_H__ */
